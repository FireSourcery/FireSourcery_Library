``` mermaid
flowchart TB
    subgraph ISR["Pin ISR: bounded, deterministic work"]
        AB["A/B quadrature edge"] --> Q["Update raw mechanical angle and edge timing"]
        Z["Z index edge"] --> I["Set raw angle to fixed index reference\nPublish index event"]
    end

    subgraph Control["PWM / state-machine context"]
        I --> S["Index search completes"]
        L["Apply known fixed d-axis field\nand wait for rotor settle"] --> C["Capture raw angle as\ncommutation zero offset"]
        Q --> C
        C --> P["Persist offset and validity"]
        S --> U["Use previously persisted\ncommutation zero offset"]
    end

    Q --> E["Compute electrical angle:\ntheta_e = direction * polePairs *\n(theta_m - theta_zero_m)"]
    P --> E
    U --> E
```